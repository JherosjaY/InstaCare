<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout 
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/dashboard_background">

    <!-- Fragment Container -->
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/fragmentContainer"
        android:name="com.example.instacare.HomeFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- Bottom Navigation Container with Shadow -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/bottomNavContainer"
        style="@style/BottomNavCardStyle"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:layout_gravity="bottom">

        <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/bottomNav"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@android:color/transparent"
            app:menu="@menu/menu_bottom_nav"
            app:labelVisibilityMode="selected"
            app:itemIconTint="@color/bottom_nav_icon_color"
            app:itemTextColor="@color/bottom_nav_icon_color"
            app:itemActiveIndicatorStyle="@null"
            app:itemRippleColor="@android:color/transparent"
            android:elevation="0dp"/>
    </com.google.android.material.card.MaterialCardView>

    <!-- Floating SOS Button (Circular with Text) -->
    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/fabSOS"
        android:layout_width="72dp"
        android:layout_height="72dp"
        android:text="SOS"
        android:textColor="@android:color/white"
        android:textStyle="bold"
        android:textSize="20sp"
        android:gravity="center"
        android:background="@drawable/circle_red_glow"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginBottom="44dp"
        android:elevation="16dp"
        android:outlineAmbientShadowColor="@color/primary"
        android:outlineSpotShadowColor="@color/primary"
        android:stateListAnimator="@null"/>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
